import styled from "styled-components";

interface TagsProps {
  tagName: string;
}

const Tags = ({ tagName }: TagsProps) => {
  return (
    <div>
      <span></span>
    </div>
  );
};

export default Tags;
